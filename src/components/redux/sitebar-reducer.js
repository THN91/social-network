let initialState = {
    friends: [
        {id: '1', name: 'Vlad'},
        {id: '2', name: 'Dima'},
        {id: '3', name: 'Vitalic'}
    ]
};

const sitebarReducer = (state = initialState, action) => {

    return state
};

export default sitebarReducer;